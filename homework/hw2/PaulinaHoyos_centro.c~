#include <stdio.h>
#include <math.h>

main(int argc, char * argv []){

  /*arrays for the group of points in the three corrdinates*/
  float *arr_x = (float *)malloc(count_lines(argv[1]) * sizeof(float));
  float *arr_y = (float *)malloc(count_lines(argv[1]) * sizeof(float));
  float *arr_z = (float *)malloc(count_lines(argv[1]) * sizeof(float));

  printf(in "%d\n" count_lines("3");

  return 0;
}


/* function that counts the number of lines in the input file*/
int  count_lines(char* file){

  FILE *in = fopen(file, 'r');
  fscanf(in,"%d",&x);


}
